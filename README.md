# saasdirectkit-plugin-template

To install dependencies:

```bash
bun add package-name
```

To run:

1) Set the path to your main saasdirectkit in `plugin.config.json` file.
2)
```bash
bun run dev
```
3) go to your saasdirectkit starterkit and start the dev server
